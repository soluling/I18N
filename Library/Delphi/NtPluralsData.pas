unit NtPluralsData;  //FI:ignore
// Generated from CLDR data. Do not edit.

interface

implementation

uses
  NtPattern;

initialization
  // Bamanankan
  TMultiPattern.Register(
    'bm',
    [pfOther],
    [pfOther],
    '0');

  // Tibetan
  TMultiPattern.Register(
    'bo',
    [pfOther],
    [pfOther],
    '0');

  // Dzongkha
  TMultiPattern.Register(
    'dz',
    [pfOther],
    [pfOther],
    '0');

  // Indonesian
  TMultiPattern.Register(
    'id',
    [pfOther],
    [pfOther],
    '0');

  // Igbo
  TMultiPattern.Register(
    'ig',
    [pfOther],
    [pfOther],
    '0');

  // Yi
  TMultiPattern.Register(
    'ii',
    [pfOther],
    [pfOther],
    '0');

  // in
  TMultiPattern.Register(
    'in',
    [pfOther],
    [pfOther],
    '0');

  // Japanese
  TMultiPattern.Register(
    'ja',
    [pfOther],
    [pfOther],
    '0');

  // jbo
  TMultiPattern.Register(
    'jbo',
    [pfOther],
    [pfOther],
    '0');

  // Javanese
  TMultiPattern.Register(
    'jv',
    [pfOther],
    [pfOther],
    '0');

  // jw
  TMultiPattern.Register(
    'jw',
    [pfOther],
    [pfOther],
    '0');

  // Makonde
  TMultiPattern.Register(
    'kde',
    [pfOther],
    [pfOther],
    '0');

  // Kabuverdianu
  TMultiPattern.Register(
    'kea',
    [pfOther],
    [pfOther],
    '0');

  // Khmer
  TMultiPattern.Register(
    'km',
    [pfOther],
    [pfOther],
    '0');

  // Korean
  TMultiPattern.Register(
    'ko',
    [pfOther],
    [pfOther],
    '0');

  // Lakota
  TMultiPattern.Register(
    'lkt',
    [pfOther],
    [pfOther],
    '0');

  // Lao
  TMultiPattern.Register(
    'lo',
    [pfOther],
    [pfOther],
    '0');

  // Malay
  TMultiPattern.Register(
    'ms',
    [pfOther],
    [pfOther],
    '0');

  // Burmese
  TMultiPattern.Register(
    'my',
    [pfOther],
    [pfOther],
    '0');

  // N'ko
  TMultiPattern.Register(
    'nqo',
    [pfOther],
    [pfOther],
    '0');

  // root
  TMultiPattern.Register(
    'root',
    [pfOther],
    [pfOther],
    '0');

  // Sakha
  TMultiPattern.Register(
    'sah',
    [pfOther],
    [pfOther],
    '0');

  // Koyraboro Senni
  TMultiPattern.Register(
    'ses',
    [pfOther],
    [pfOther],
    '0');

  // Sango
  TMultiPattern.Register(
    'sg',
    [pfOther],
    [pfOther],
    '0');

  // Thai
  TMultiPattern.Register(
    'th',
    [pfOther],
    [pfOther],
    '0');

  // Tongan
  TMultiPattern.Register(
    'to',
    [pfOther],
    [pfOther],
    '0');

  // Vietnamese
  TMultiPattern.Register(
    'vi',
    [pfOther],
    [pfOther],
    '0');

  // Wolof
  TMultiPattern.Register(
    'wo',
    [pfOther],
    [pfOther],
    '0');

  // Yoruba
  TMultiPattern.Register(
    'yo',
    [pfOther],
    [pfOther],
    '0');

  // yue
  TMultiPattern.Register(
    'yue',
    [pfOther],
    [pfOther],
    '0');

  // Chinese, Simplified
  TMultiPattern.Register(
    'zh',
    [pfOther],
    [pfOther],
    '0');

  // Amharic
  TMultiPattern.Register(
    'am',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // Assamese
  TMultiPattern.Register(
    'as',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // Bangla
  TMultiPattern.Register(
    'bn',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // Persian
  TMultiPattern.Register(
    'fa',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // Gujarati
  TMultiPattern.Register(
    'gu',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // Hindi
  TMultiPattern.Register(
    'hi',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // Kannada
  TMultiPattern.Register(
    'kn',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // Marathi
  TMultiPattern.Register(
    'mr',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // isiZulu
  TMultiPattern.Register(
    'zu',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 0) || (n == 1) ? 0 : 1');

  // Fulah
  TMultiPattern.Register(
    'ff',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '((n == 0) || (n == 1)) ? 0 : 1');

  // French
  TMultiPattern.Register(
    'fr',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '((n == 0) || (n == 1)) ? 0 : 1');

  // Armenian
  TMultiPattern.Register(
    'hy',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '((n == 0) || (n == 1)) ? 0 : 1');

  // Kabyle
  TMultiPattern.Register(
    'kab',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '((n == 0) || (n == 1)) ? 0 : 1');

  // Asturian
  TMultiPattern.Register(
    'ast',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Catalan
  TMultiPattern.Register(
    'ca',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // German
  TMultiPattern.Register(
    'de',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // English
  TMultiPattern.Register(
    'en',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Estonian
  TMultiPattern.Register(
    'et',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Finnish
  TMultiPattern.Register(
    'fi',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Western Frisian
  TMultiPattern.Register(
    'fy',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Galician
  TMultiPattern.Register(
    'gl',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Italian
  TMultiPattern.Register(
    'it',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // ji
  TMultiPattern.Register(
    'ji',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Dutch
  TMultiPattern.Register(
    'nl',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Swedish
  TMultiPattern.Register(
    'sv',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Kiswahili
  TMultiPattern.Register(
    'sw',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Urdu
  TMultiPattern.Register(
    'ur',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Yiddish
  TMultiPattern.Register(
    'yi',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) ? 0 : 1');

  // Sinhala
  TMultiPattern.Register(
    'si',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '((n == 0) || (n == 1)) ? 0 : 1');

  // Akan
  TMultiPattern.Register(
    'ak',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // bh
  TMultiPattern.Register(
    'bh',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // guw
  TMultiPattern.Register(
    'guw',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // Lingala
  TMultiPattern.Register(
    'ln',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // Malagasy
  TMultiPattern.Register(
    'mg',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // Sesotho sa Leboa
  TMultiPattern.Register(
    'nso',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // Punjabi
  TMultiPattern.Register(
    'pa',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // Tigrinya
  TMultiPattern.Register(
    'ti',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // wa
  TMultiPattern.Register(
    'wa',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // Central Atlas Tamazight
  TMultiPattern.Register(
    'tzm',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) || (n >= 11) && (n <= 99) ? 0 : 1');

  // Portuguese
  TMultiPattern.Register(
    'pt',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n >= 0) && (n <= 1) ? 0 : 1');

  // Afrikaans
  TMultiPattern.Register(
    'af',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Asu
  TMultiPattern.Register(
    'asa',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Azerbaijani, Latin
  TMultiPattern.Register(
    'az',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Bemba
  TMultiPattern.Register(
    'bem',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Bena
  TMultiPattern.Register(
    'bez',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Bulgarian
  TMultiPattern.Register(
    'bg',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Bodo
  TMultiPattern.Register(
    'brx',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Chechen
  TMultiPattern.Register(
    'ce',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Chiga
  TMultiPattern.Register(
    'cgg',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Cherokee
  TMultiPattern.Register(
    'chr',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // ckb
  TMultiPattern.Register(
    'ckb',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Divehi
  TMultiPattern.Register(
    'dv',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Ewe
  TMultiPattern.Register(
    'ee',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Greek
  TMultiPattern.Register(
    'el',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Esperanto
  TMultiPattern.Register(
    'eo',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Spanish
  TMultiPattern.Register(
    'es',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Basque
  TMultiPattern.Register(
    'eu',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Faroese
  TMultiPattern.Register(
    'fo',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Friulian
  TMultiPattern.Register(
    'fur',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Swiss German
  TMultiPattern.Register(
    'gsw',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Hausa
  TMultiPattern.Register(
    'ha',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Hawaiian
  TMultiPattern.Register(
    'haw',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Hungarian
  TMultiPattern.Register(
    'hu',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Ngomba
  TMultiPattern.Register(
    'jgo',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Machame
  TMultiPattern.Register(
    'jmc',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Georgian
  TMultiPattern.Register(
    'ka',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // kaj
  TMultiPattern.Register(
    'kaj',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // kcg
  TMultiPattern.Register(
    'kcg',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Kazakh
  TMultiPattern.Register(
    'kk',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Kako
  TMultiPattern.Register(
    'kkj',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Greenlandic
  TMultiPattern.Register(
    'kl',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Kashmiri
  TMultiPattern.Register(
    'ks',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Shambala
  TMultiPattern.Register(
    'ksb',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Central Kurdish
  TMultiPattern.Register(
    'ku',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Kyrgyz
  TMultiPattern.Register(
    'ky',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Luxembourgish
  TMultiPattern.Register(
    'lb',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Ganda
  TMultiPattern.Register(
    'lg',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Masai
  TMultiPattern.Register(
    'mas',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Meta'
  TMultiPattern.Register(
    'mgo',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Malayalam
  TMultiPattern.Register(
    'ml',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Mongolian, Cyrillic
  TMultiPattern.Register(
    'mn',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // nah
  TMultiPattern.Register(
    'nah',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Norwegian Bokmål
  TMultiPattern.Register(
    'nb',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // North Ndebele
  TMultiPattern.Register(
    'nd',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Nepali
  TMultiPattern.Register(
    'ne',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Norwegian Nynorsk
  TMultiPattern.Register(
    'nn',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Ngiemboon
  TMultiPattern.Register(
    'nnh',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Norwegian
  TMultiPattern.Register(
    'no',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // South Ndebele
  TMultiPattern.Register(
    'nr',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // ny
  TMultiPattern.Register(
    'ny',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Nyankole
  TMultiPattern.Register(
    'nyn',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Oromo
  TMultiPattern.Register(
    'om',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Odia
  TMultiPattern.Register(
    'or',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Ossetic
  TMultiPattern.Register(
    'os',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Papiamento
  TMultiPattern.Register(
    'pap',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Pashto
  TMultiPattern.Register(
    'ps',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Romansh
  TMultiPattern.Register(
    'rm',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Rombo
  TMultiPattern.Register(
    'rof',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Rwa
  TMultiPattern.Register(
    'rwk',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Samburu
  TMultiPattern.Register(
    'saq',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // sdh
  TMultiPattern.Register(
    'sdh',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Sena
  TMultiPattern.Register(
    'seh',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Shona
  TMultiPattern.Register(
    'sn',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Somali
  TMultiPattern.Register(
    'so',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Albanian
  TMultiPattern.Register(
    'sq',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // siSwati
  TMultiPattern.Register(
    'ss',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Saho
  TMultiPattern.Register(
    'ssy',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Sesotho
  TMultiPattern.Register(
    'st',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Syriac
  TMultiPattern.Register(
    'syr',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Tamil
  TMultiPattern.Register(
    'ta',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Telugu
  TMultiPattern.Register(
    'te',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Teso
  TMultiPattern.Register(
    'teo',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Tigre
  TMultiPattern.Register(
    'tig',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Turkmen
  TMultiPattern.Register(
    'tk',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Setswana
  TMultiPattern.Register(
    'tn',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Turkish
  TMultiPattern.Register(
    'tr',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Tsonga
  TMultiPattern.Register(
    'ts',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Uyghur
  TMultiPattern.Register(
    'ug',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Uzbek, Latin
  TMultiPattern.Register(
    'uz',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Venda
  TMultiPattern.Register(
    've',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Volapük
  TMultiPattern.Register(
    'vo',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Vunjo
  TMultiPattern.Register(
    'vun',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Walser
  TMultiPattern.Register(
    'wae',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // isiXhosa
  TMultiPattern.Register(
    'xh',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Soga
  TMultiPattern.Register(
    'xog',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Portuguese (Portugal)
  TMultiPattern.Register(
    'pt-PT',
    [pfOne, pfOther],
    [pfOther],
    '(n == 1) && (0 == 0) ? 0 : 1');

  // Danish
  TMultiPattern.Register(
    'da',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n == 1) ? 0 : 1');

  // Icelandic
  TMultiPattern.Register(
    'is',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(n % 10 == 1) && (n % 100 != 11) ? 0 : 1');

  // Macedonian
  TMultiPattern.Register(
    'mk',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(0 == 0) && (n % 10 == 1) ? 0 : 1');

  // Filipino
  TMultiPattern.Register(
    'fil',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(0 == 0) && ((n == 1) || (n == 2) || (n == 3)) || (0 == 0) && ((n % 10 != 4) || (n % 10 != 6) || (n % 10 != 9)) ? 0 : 1');

  // tl
  TMultiPattern.Register(
    'tl',
    [pfOne, pfOther],
    [pfOne, pfOther],
    '(0 == 0) && ((n == 1) || (n == 2) || (n == 3)) || (0 == 0) && ((n % 10 != 4) || (n % 10 != 6) || (n % 10 != 9)) ? 0 : 1');

  // Latvian
  TMultiPattern.Register(
    'lv',
    [pfZero, pfOne, pfOther],
    [pfZero, pfOne, pfOther],
    '(n % 10 == 0) || (n % 100 >= 11) && (n % 100 <= 19) ? 0 : (n % 10 == 1) && (n % 100 != 11) ? 1 : 2');

  // Prussian
  TMultiPattern.Register(
    'prg',
    [pfZero, pfOne, pfOther],
    [pfZero, pfOne, pfOther],
    '(n % 10 == 0) || (n % 100 >= 11) && (n % 100 <= 19) ? 0 : (n % 10 == 1) && (n % 100 != 11) ? 1 : 2');

  // Langi
  TMultiPattern.Register(
    'lag',
    [pfZero, pfOne, pfOther],
    [pfZero, pfOne, pfOther],
    '(n == 0) ? 0 : ((n == 0) || (n == 1)) && (n != 0) ? 1 : 2');

  // Colognian
  TMultiPattern.Register(
    'ksh',
    [pfZero, pfOne, pfOther],
    [pfZero, pfOne, pfOther],
    '(n == 0) ? 0 : (n == 1) ? 1 : 2');

  // Inuktitut, Latin
  TMultiPattern.Register(
    'iu',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // Cornish
  TMultiPattern.Register(
    'kw',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // Nama
  TMultiPattern.Register(
    'naq',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // Northern Sami
  TMultiPattern.Register(
    'se',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // Sami, Southern
  TMultiPattern.Register(
    'sma',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // smi
  TMultiPattern.Register(
    'smi',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // Sami, Lule
  TMultiPattern.Register(
    'smj',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // Sami, Inari
  TMultiPattern.Register(
    'smn',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // Sami, Skolt
  TMultiPattern.Register(
    'sms',
    [pfOne, pfTwo, pfOther],
    [pfOne, pfTwo, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : 2');

  // Tachelhit
  TMultiPattern.Register(
    'shi',
    [pfOne, pfFew, pfOther],
    [pfOne, pfFew, pfOther],
    '(n == 0) || (n == 1) ? 0 : (n >= 2) && (n <= 10) ? 1 : 2');

  // mo
  TMultiPattern.Register(
    'mo',
    [pfOne, pfFew, pfOther],
    [pfFew],
    '(n == 1) ? 0 : (0 != 0) || (n == 0) || (n != 1) && (n % 100 >= 1) && (n % 100 <= 19) ? 1 : 2');

  // Romanian
  TMultiPattern.Register(
    'ro',
    [pfOne, pfFew, pfOther],
    [pfFew],
    '(n == 1) ? 0 : (0 != 0) || (n == 0) || (n != 1) && (n % 100 >= 1) && (n % 100 <= 19) ? 1 : 2');

  // Bosnian, Latin
  TMultiPattern.Register(
    'bs',
    [pfOne, pfFew, pfOther],
    [pfOne, pfFew, pfOther],
    '(0 == 0) && (n % 10 == 1) && (n % 100 != 11) ? 0 : (0 == 0) && (n % 10 >= 2) && (n % 10 <= 4) && (n % 100 >= 12) && (n % 100 <= 14) ? 1 : 2');

  // Croatian
  TMultiPattern.Register(
    'hr',
    [pfOne, pfFew, pfOther],
    [pfOne, pfFew, pfOther],
    '(0 == 0) && (n % 10 == 1) && (n % 100 != 11) ? 0 : (0 == 0) && (n % 10 >= 2) && (n % 10 <= 4) && (n % 100 >= 12) && (n % 100 <= 14) ? 1 : 2');

  // sh
  TMultiPattern.Register(
    'sh',
    [pfOne, pfFew, pfOther],
    [pfOne, pfFew, pfOther],
    '(0 == 0) && (n % 10 == 1) && (n % 100 != 11) ? 0 : (0 == 0) && (n % 10 >= 2) && (n % 10 <= 4) && (n % 100 >= 12) && (n % 100 <= 14) ? 1 : 2');

  // Serbian, Cyrillic
  TMultiPattern.Register(
    'sr',
    [pfOne, pfFew, pfOther],
    [pfOne, pfFew, pfOther],
    '(0 == 0) && (n % 10 == 1) && (n % 100 != 11) ? 0 : (0 == 0) && (n % 10 >= 2) && (n % 10 <= 4) && (n % 100 >= 12) && (n % 100 <= 14) ? 1 : 2');

  // Scottish Gaelic
  TMultiPattern.Register(
    'gd',
    [pfOne, pfTwo, pfFew, pfOther],
    [pfOne, pfTwo, pfFew, pfOther],
    '((n == 1) || (n == 11)) ? 0 : ((n == 2) || (n == 12)) ? 1 : ((n >= 3) && (n >= 13) && (n <= 19)) ? 2 : 3');

  // Slovenian
  TMultiPattern.Register(
    'sl',
    [pfOne, pfTwo, pfFew, pfOther],
    [pfFew],
    '(0 == 0) && (n % 100 == 1) ? 0 : (0 == 0) && (n % 100 == 2) ? 1 : (0 == 0) && (n % 100 >= 3) && (n % 100 <= 4) || (0 != 0) ? 2 : 3');

  // Lower Sorbian
  TMultiPattern.Register(
    'dsb',
    [pfOne, pfTwo, pfFew, pfOther],
    [pfOne, pfTwo, pfFew, pfOther],
    '(0 == 0) && (n % 100 == 1) ? 0 : (0 == 0) && (n % 100 == 2) ? 1 : (0 == 0) && (n % 100 >= 3) && (n % 100 <= 4) ? 2 : 3');

  // Upper Sorbian
  TMultiPattern.Register(
    'hsb',
    [pfOne, pfTwo, pfFew, pfOther],
    [pfOne, pfTwo, pfFew, pfOther],
    '(0 == 0) && (n % 100 == 1) ? 0 : (0 == 0) && (n % 100 == 2) ? 1 : (0 == 0) && (n % 100 >= 3) && (n % 100 <= 4) ? 2 : 3');

  // Hebrew
  TMultiPattern.Register(
    'he',
    [pfOne, pfTwo, pfMany, pfOther],
    [pfOther],
    '(n == 1) ? 0 : (n == 2) && (0 == 0) ? 1 : (0 == 0) && (n >= 0) && (n <= 10) && (n % 10 == 0) ? 2 : 3');

  // iw
  TMultiPattern.Register(
    'iw',
    [pfOne, pfTwo, pfMany, pfOther],
    [pfOther],
    '(n == 1) ? 0 : (n == 2) && (0 == 0) ? 1 : (0 == 0) && (n >= 0) && (n <= 10) && (n % 10 == 0) ? 2 : 3');

  // Czech
  TMultiPattern.Register(
    'cs',
    [pfOne, pfFew, pfOther],
    [pfMany],
    '(n == 1) ? 0 : (n >= 2) && (n <= 4) && (0 == 0) ? 1 : 2');

  // Slovak
  TMultiPattern.Register(
    'sk',
    [pfOne, pfFew, pfOther],
    [pfMany],
    '(n == 1) ? 0 : (n >= 2) && (n <= 4) && (0 == 0) ? 1 : 2');

  // Polish
  TMultiPattern.Register(
    'pl',
    [pfOne, pfFew, pfMany],
    [pfOther],
    '(n == 1) ? 0 : (0 == 0) && (n % 10 >= 2) && (n % 10 <= 4) && (n % 100 >= 12) && (n % 100 <= 14) ? 1 : 2');

  // Belarusian
  TMultiPattern.Register(
    'be',
    [pfOne, pfFew, pfMany],
    [pfOne, pfFew, pfMany, pfOther],
    '(n % 10 == 1) && (n % 100 != 11) ? 0 : (n % 10 >= 2) && (n % 10 <= 4) && (n % 100 >= 12) && (n % 100 <= 14) ? 1 : 2');

  // Lithuanian
  TMultiPattern.Register(
    'lt',
    [pfOne, pfFew, pfOther],
    [pfOne, pfFew, pfMany, pfOther],
    '(n % 10 == 1) && (n % 100 >= 11) && (n % 100 <= 19) ? 0 : (n % 10 >= 2) && (n % 10 <= 9) && (n % 100 >= 11) && (n % 100 <= 19) ? 1 : 2');

  // Maltese
  TMultiPattern.Register(
    'mt',
    [pfOne, pfFew, pfMany, pfOther],
    [pfOne, pfFew, pfMany, pfOther],
    '(n == 1) ? 0 : (n == 0) || (n % 100 >= 2) && (n % 100 <= 10) ? 1 : (n % 100 >= 11) && (n % 100 <= 19) ? 2 : 3');

  // Russian
  TMultiPattern.Register(
    'ru',
    [pfOne, pfFew, pfMany],
    [pfOther],
    '(0 == 0) && (n % 10 == 1) && (n % 100 != 11) ? 0 : (0 == 0) && (n % 10 >= 2) && (n % 10 <= 4) && (n % 100 >= 12) && (n % 100 <= 14) ? 1 : 2');

  // Ukrainian
  TMultiPattern.Register(
    'uk',
    [pfOne, pfFew, pfMany],
    [pfOther],
    '(0 == 0) && (n % 10 == 1) && (n % 100 != 11) ? 0 : (0 == 0) && (n % 10 >= 2) && (n % 10 <= 4) && (n % 100 >= 12) && (n % 100 <= 14) ? 1 : 2');

  // Breton
  TMultiPattern.Register(
    'br',
    [pfOne, pfTwo, pfFew, pfMany, pfOther],
    [pfOne, pfTwo, pfFew, pfMany, pfOther],
    '(n % 10 == 1) && ((n % 100 != 11) || (n % 100 != 71) || (n % 100 != 91)) ? 0 : (n % 10 == 2) && ((n % 100 != 12) || (n % 100 != 72) || (n % 100 != 92)) ? 1 : ((n % 10 >= 3) && (n % 10 <= 4) || (n % 10' + ' == 9)) && ((n % 100 >= 10) && (n % 100 >= 70) && (n % 100 >= 90) && (n % 100 <= 99)) ? 2 : (n != 0) && (n % 1000000 == 0) ? 3 : 4');

  // Irish
  TMultiPattern.Register(
    'ga',
    [pfOne, pfTwo, pfFew, pfMany, pfOther],
    [pfOne, pfTwo, pfFew, pfMany, pfOther],
    '(n == 1) ? 0 : (n == 2) ? 1 : (n >= 3) && (n <= 6) ? 2 : (n >= 7) && (n <= 10) ? 3 : 4');

  // Manx
  TMultiPattern.Register(
    'gv',
    [pfOne, pfTwo, pfFew, pfOther],
    [pfMany],
    '(0 == 0) && (n % 10 == 1) ? 0 : (0 == 0) && (n % 10 == 2) ? 1 : (0 == 0) && ((n % 100 == 0) || (n % 100 == 20) || (n % 100 == 40) || (n % 100 == 60) || (n % 100 == 80)) ? 2 : 3');

  // Arabic
  TMultiPattern.Register(
    'ar',
    [pfZero, pfOne, pfTwo, pfFew, pfMany, pfOther],
    [pfZero, pfOne, pfTwo, pfFew, pfMany, pfOther],
    '(n == 0) ? 0 : (n == 1) ? 1 : (n == 2) ? 2 : (n % 100 >= 3) && (n % 100 <= 10) ? 3 : (n % 100 >= 11) && (n % 100 <= 99) ? 4 : 5');

  // ars
  TMultiPattern.Register(
    'ars',
    [pfZero, pfOne, pfTwo, pfFew, pfMany, pfOther],
    [pfZero, pfOne, pfTwo, pfFew, pfMany, pfOther],
    '(n == 0) ? 0 : (n == 1) ? 1 : (n == 2) ? 2 : (n % 100 >= 3) && (n % 100 <= 10) ? 3 : (n % 100 >= 11) && (n % 100 <= 99) ? 4 : 5');

  // Welsh
  TMultiPattern.Register(
    'cy',
    [pfZero, pfOne, pfTwo, pfFew, pfMany, pfOther],
    [pfZero, pfOne, pfTwo, pfFew, pfMany, pfOther],
    '(n == 0) ? 0 : (n == 1) ? 1 : (n == 2) ? 2 : (n == 3) ? 3 : (n == 6) ? 4 : 5');

end.
